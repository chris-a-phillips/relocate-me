import React, { useState, useEffect } from 'react';

const Details = ({ chosenCity }) => {
  console.log(chosenCity)

  // useEffect(() => {
  //   chosenCity.map(category.id => {
  //     stuff=(category.id)
  //   })
  // }, [])

  return (
    <div>
      
    </div>
  );
};

export default Details;