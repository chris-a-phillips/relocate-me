import React from 'react';

const NavBar = () => {
  return (
    <div>
      <div>
        <span>Logo</span>
        <span>Home</span>
        <span>Dropdown</span>
        <input type='text'></input>
        <button>Search</button>
      </div>
    </div>
  );
};

export default NavBar;